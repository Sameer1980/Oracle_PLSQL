CREATE OR REPLACE FUNCTION SF_GET_JOB_AVGSAL(P_JOBID VARCHAR2) RETURN NUMBER AS
V_AVGSAL NUMBER(5);
BEGIN

SELECT AVG(SALARY) INTO V_AVGSAL FROM EMPLOYEES WHERE JOB_ID=P_JOBID;

RETURN V_AVGSAL;

END;
/
SHOW ERRORS;
