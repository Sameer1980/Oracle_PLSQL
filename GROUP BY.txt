SELECT 
 A.EMPLOYEE_ID,
 INITCAP(A.FIRST_NAME||' '||A.LAST_NAME)"NAME",
 B.JID,
 B.SAL
 FROM 
 EMPLOYEES A,
 (SELECT JOB_ID "JID",AVG(SALARY) "SAL" FROM EMPLOYEES GROUP BY JOB_ID)B
 WHERE
 A.JOB_ID=B.JID  
  AND B.SAL=(SELECT MIN(AVG(SALARY)) FROM EMPLOYEES GROUP BY JOB_ID)