SELECT * FROM
(
(
SELECT DEPARTMENT_ID,
JOB_ID
FROM
EMPLOYEES 
)
MINUS
(
SELECT DEPARTMENT_ID,
JOB_ID
FROM
EMPLOYEES 
WHERE DEPARTMENT_ID IN(SELECT DEPARTMENT_ID FROM EMPLOYEES WHERE JOB_ID='ST_CLERK')
)
)
ORDER BY 1