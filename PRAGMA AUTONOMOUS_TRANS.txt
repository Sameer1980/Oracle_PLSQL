CREATE TABLE LOG_USAGE 
( CARDNO NUMBER(8,2) PRIMARY KEY,
  LOC VARCHAR2(80) NOT NULL
 ) 
 
-- ALTER TABLE LOG_USAGE RENAME TO TAXES
 
 
 
 CREATE TABLE USAGE 
 (
 CARD_ID NUMBER(8,2) PRIMARY KEY,
 LOC VARCHAR2(80) NOT NULL
 )
 
 CREATE OR REPLACE PROCEDURE BANK_TRANS(CARDNO NUMBER,LOC VARCHAR2) AS
 BEGIN
 LOG_USAGE (CARDNO,LOC);
 INSERT INTO TAXES VALUES (CARDNO,LOC);
 END;
 
 CREATE OR REPLACE PROCEDURE LOG_USAGE (CARD_ID NUMBER, LOC VARCHAR2) AS
 PRAGMA AUTONOMOUS_TRANSACTION;
 BEGIN
 INSERT INTO USAGE 
 VALUES (CARD_ID,LOC);
 COMMIT;
 END ;
 
 EXEC BANK_TRANS(101,'PAK');