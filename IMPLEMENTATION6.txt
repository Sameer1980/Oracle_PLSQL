CREATE OR REPLACE TRIGGER RESTRICT_SALARY
BEFORE INSERT OR UPDATE OF SALARY ON DEMO_EMP REFERENCING OLD AS O NEW AS N
FOR EACH ROW
BEGIN
IF NOT (:N.JOB_ID IN('AD_PRES','AD_VP'))AND :N.SALARY>15000 THEN
RAISE_APPLICATION_ERROR(-20202,'EMPLOYEE CANNOT EARN MORE THAN $15000.');
END IF;
END;