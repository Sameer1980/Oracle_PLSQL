CREATE OR REPLACE PROCEDURE SP_NEW_JOB (J_ID VARCHAR2,J_TITLE VARCHAR2,MIN_SAL NUMBER) AS
BEGIN
INSERT INTO JOBS(JOB_ID,JOB_TITLE,MIN_SALARY,MAX_SALARY)
VALUES(J_ID,J_TITLE,MIN_SAL,MIN_SAL * 2);
COMMIT;
END;
/
SHOW ERRORS;