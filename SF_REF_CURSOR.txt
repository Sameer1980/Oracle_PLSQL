CREATE OR REPLACE FUNCTION DEMO_SF_REF_CURSOR RETURN SYS_REFCURSOR
AS
  EMP SYS_REFCURSOR;
BEGIN
  OPEN EMP FOR SELECT * FROM EMPLOYEES A,DEPARTMENTS B WHERE A.DEPARTMENT_ID=B.DEPARTMENT_ID(+);
  RETURN EMP;
END;
