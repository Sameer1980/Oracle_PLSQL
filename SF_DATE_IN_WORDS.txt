CREATE OR REPLACE FUNCTION SF_DATE_IN_WORDS (P_DATE DATE) RETURN VARCHAR AS 

V_DATE VARCHAR2(100);

BEGIN

SELECT TO_CHAR(P_DATE,'Day,"the" Ddthsp "of" Month,yyyy') INTO V_DATE FROM DUAL;

RETURN(V_DATE);

END;