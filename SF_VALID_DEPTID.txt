CREATE OR REPLACE FUNCTION VALID_DEPTID (P_DEPTID NUMBER) RETURN BOOLEAN IS
V_COUNT NUMBER;
BEGIN
SELECT COUNT(*) INTO V_COUNT FROM DEPARTMENTS WHERE DEPARTMENT_ID=P_DEPTID;

IF V_COUNT=0 THEN 

RETURN FALSE;
ELSE
RETURN TRUE;

END IF;
END; 