DECLARE
JID VARCHAR2(30);
JNAME VARCHAR2(55);
BEGIN
SELECT JOB_ID,JOB_TITLE INTO JID,JNAME FROM JOBS  WHERE JOB_ID='IT_WEB';
DBMS_OUTPUT.PUT_LINE(JID || ' '||JNAME);

EXCEPTION
WHEN NO_DATA_FOUND THEN
DBMS_OUTPUT.PUT_LINE(SQLCODE);
DBMS_OUTPUT.PUT_LINE(SQLERRM);
END;